/**
 * 原生端不支持 CSS。这个模块存在的唯一意义，是让 `@/global-css` 在非 web 平台解析到一个空
 * 实现——真正引入全局样式的是同目录的 `global-css.web.ts`（Metro 按平台后缀自动选择）。
 *
 * 直接在各平台无条件 `import '@/global.css'` 也能过 web，但原生打包器会去解析这个 CSS 文件，
 * 属于给自己埋雷，所以走平台后缀这条既有的路子。
 */
export {};
